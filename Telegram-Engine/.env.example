# ─── Database (PostgreSQL) ────────────────────────────────
DATABASE_URL="postgresql://telegram:telegram_secret@localhost:5432/telegram_engine?schema=public"

# ─── Redis (BullMQ + rate limiting) ──────────────────────
REDIS_URL="redis://localhost:6379"

# ─── JWT Authentication ──────────────────────────────────
JWT_SECRET="change-me-to-a-long-random-string-min-32-chars"

# ─── Telegram Bot ────────────────────────────────────────
TELEGRAM_BOT_TOKEN="123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11"

# ─── Firebase Admin (read-only subscriber sync) ─────────
# Path to service account JSON for SOS Urgently project
FIREBASE_SERVICE_ACCOUNT_PATH="./firebase-service-account.json"

# ─── Server ──────────────────────────────────────────────
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:5176

# ─── Docker Postgres (used by docker-compose) ───────────
POSTGRES_USER=telegram
POSTGRES_PASSWORD=telegram_secret
POSTGRES_DB=telegram_engine
